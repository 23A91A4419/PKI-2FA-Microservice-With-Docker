PKI-Based 2FA Microservice

This project implements a secure microservice using Public Key Infrastructure (PKI) and Time-Based One-Time Passwords (TOTP). It demonstrates encrypted seed retrieval, seed decryption, TOTP generation, secure API endpoints, and automated cron-based 2FA logging inside a Dockerized environment.
